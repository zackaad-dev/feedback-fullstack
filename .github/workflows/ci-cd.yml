name: CI/CD Pipeline

on:
  push:
    branches: [ main, dev ]
  pull_request:
    branches: [ main, dev ]

jobs:
  # ==========================================
  # CI Jobs: Run on both `dev` and `main`
  # ==========================================
  backend-ci:
    name: Backend CI (Test & Lint)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./backend

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: './backend/package-lock.json'

      - name: Install Dependencies
        run: npm ci || npm install

      - name: Run Backend Tests & Syntax Checks
        run: npm test

  frontend-ci:
    name: Frontend CI (Lint & Build)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./frontend

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: './frontend/package-lock.json'

      - name: Install Dependencies
        run: npm ci || npm install

      - name: Run Frontend Linter
        run: npm run lint

      - name: Build Frontend Production Bundle
        run: npm run build

  # ==========================================
  # CD Job: Runs ONLY on `main` branch push
  # ==========================================
  deploy-main:
    name: Deploy Production (Main Branch)
    needs: [backend-ci, frontend-ci]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Production Deployment Step
        run: |
          echo "Deploying application from main branch..."
          # Add deployment commands here (e.g. Docker build & push, AWS, Vercel, Render, etc.)
